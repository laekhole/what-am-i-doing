# waid — 프로젝트 매니페스트 (개정 3판)

> 현재 버전 정의는 [README.md의 로드맵](README.md#version-roadmap)이 우선합니다. v0.2.0은 Windows 사용 가능 단계, v0.3.0은 macOS, v0.4.0은 Android·waidaway LAN, v0.5.0은 iOS·iPadOS, v1.0.0은 안정화 완료입니다. 아래 과거 UI·스키마 수치는 당시 설계 기록이며 현재 기능을 제한하지 않습니다.

> **what am I doing?**
> 에이전트 다섯 개를 돌려놓고 "내가 지금 뭘 하고 있더라" 하는 그 순간을 위한 도구.
> 저장소: `what-am-i-doing` · 바이너리·크레이트: `waid`

---

## 0. 한 줄 정의

돌고 있는 코딩 에이전트를 **자동으로 찾아내**, 다섯 개 컬럼으로 보여준다. 그 외에는 아무것도 하지 않는다.

**대상 플랫폼:** Windows → macOS → Android 순. Linux는 코어가 이미 동작하므로 유지한다.
**형태:** 작은 네이티브 상태 메모 + 그 안에 들어 있는 독립 CLI 코어. 브라우저 기반 앱은 사용하지 않는다.

### 0.1 1판에서 무엇이 바뀌었나

1판은 "단일 머신의 터미널 도구"였다. 2판은 "여러 실행 환경을 가로지르는 데스크톱 앱"이다. 세 가지가 바뀌었고, 각각 대가가 있다.

| 바뀐 것 | 이유 | 대가 |
|---|---|---|
| 터미널 표 → 설치형 GUI | 대상 사용자가 터미널 바깥에 있다 | 외부 의존성 0개가 **껍데기에서는** 깨진다 |
| 단일 머신 → 원격 열람 허용 | Android에서 보려면 다른 길이 없다 | 인증·전송 보안이 새 책임으로 들어온다 |
| 프로세스 1차 → 트랜스크립트 1차 | 프로세스는 WSL 경계를 못 넘는다 | 상태 판정이 파일 기반 추론에 더 의존한다 |

**바뀌지 않은 것이 더 중요하다.** §1.1(관찰만 한다), §1.2(다섯 컬럼), §1.3(코어는 렌더링하지 않는다), §1.4(설정 없이 동작한다)는 그대로다. 특히 §1.3은 이제 이 프로젝트의 척추다 — 1판에서 코어와 표시기를 갈라놓은 덕분에, GUI를 붙이는 데 코어를 한 줄도 고치지 않아도 된다.

```
TITLE              AGENT    LLM              TASK                          STATUS
api-server/auth    claude   opus-4.6         JWT 리프레시 토큰 만료 처리      ● working
web/checkout       codex    gpt-5.2          결제 폼 접근성 개선             ◐ waiting
infra              gemini   gemini-3-pro     테라폼 모듈 정리                ○ idle
docs               aider    sonnet-4.6       README 다국어화                 ✓ done
```

---

### 0.2 3판 — 화면의 재사용보다 작고 가벼운 창

사용자 확인으로 Tauri·웹뷰 껍데기를 네이티브 창으로 교체한다 (D19). 카드·배지·애니메이션 없이 다섯 필드를 간단한 텍스트 목록으로 표시한다. 최소화·닫기만 두고 최대화는 제공하지 않는다. 항상 위와 불투명도는 우클릭 메뉴로 조절한다. 닫기는 앱과 자신이 만든 코어를 종료한다.

대가는 플랫폼별 표시 코드다. §9의 "앱 전용 렌더링 0줄"은 폐기하되, 코어의 독립성·외부 크레이트 0개·다섯 필드·관찰 전용은 유지한다. HTML 출력은 CLI의 선택 기능으로 남고 앱에서는 사용하지 않는다. 트레이·토스트는 이번 최소 창의 범위에서 제외한다.

---

## 1. 철학

### 1.1 관찰하되, 개입하지 않는다

waid는 에이전트를 **실행하지 않는다**. tmux 세션을 만들지 않고, worktree를 파지 않고, 프롬프트를 보내지 않고, 컨테이너를 띄우지 않는다. 사용자는 지금까지 하던 대로 각자의 터미널에서 `claude`를 치고, waid는 그걸 옆에서 볼 뿐이다.

이것이 orca·agent-deck·agent-of-empires와의 결정적 차이다. 그들은 **세션의 소유자**가 되는 대가로 풍부한 메타데이터를 얻는다. waid는 소유권을 포기하는 대신, 설치 후 3초 만에 아무 설정 없이 동작한다.

파일 시스템은 **읽기 전용으로만** 접근한다. 에이전트의 트랜스크립트를 수정하거나 삭제하는 기능은 영원히 없다.

### 1.2 다섯 개 컬럼이 전부다

title / agent / llm / task / status. 이 다섯은 "지금 뭘 봐야 하는가"에 답하기 위한 최소 집합이다. 여섯 번째 컬럼을 추가하자는 제안은 기본적으로 거절한다.

특히 다음은 **의도적으로 넣지 않는다**:

- 토큰 사용량, 비용, 컨텍스트 사용률 — 유용하지만 이건 `ccusage`나 `agtop`의 영역이다
- CPU/메모리 그래프 — `htop`이 있다
- diff 뷰어, 로그 스트림, 파일 목록 — 그건 IDE다
- 서브에이전트 트리, MCP 서버 상태, 스킬 목록

거절 이유는 "쓸모없어서"가 아니라, **한 화면에 12개 컬럼이 있으면 아무것도 안 보이기 때문**이다.

### 1.3 코어는 렌더링하지 않는다

수집기(collector)와 표시기(renderer)를 완전히 분리한다. 코어는 스냅샷 JSON을 뱉는 것으로 임무를 끝낸다. TUI도, 웹 대시보드도, 메뉴바 위젯도 전부 그 JSON을 소비하는 동등한 클라이언트다.

이것이 "가볍게" + "디자인 커스텀 가능하게"를 동시에 만족시키는 유일한 방법이다. 플러그인 API를 만들어 코어를 뚱뚱하게 만드는 대신, **커스터마이징은 파이프 바깥에서 일어난다.**

```
waid --json | jq ... | my-own-renderer
```

2판에서 이 원칙은 선택이 아니라 생존 조건이 됐다. 데스크톱 앱은 이 JSON을 소비하는 또 하나의 클라이언트일 뿐이며, 코어에 GUI 코드가 한 줄이라도 들어가는 순간 Windows·macOS·Android를 각각 따로 만들게 된다.

### 1.4 설정 없이 동작하고, 설정으로 바뀐다

`waid` 한 단어로 즉시 쓸 만한 출력이 나와야 한다. 설정 파일은 전부 선택 사항이며, 없을 때의 기본값이 곧 권장 사양이다.

### 1.5 코어와 껍데기는 끝까지 분리된 채로 배포된다

앱을 만든다고 코어가 앱의 일부가 되는 것이 아니다. **`waid` 바이너리는 앱 없이도 단독으로 동작하며, 계속 외부 크레이트 0개를 유지한다.** 앱은 그 바이너리를 동봉해 실행하고 출력을 화면에 그리는 껍데기다.

이 경계를 무르게 두면 반드시 이런 일이 생긴다 — GUI에서 편한 어떤 값을 코어가 계산해주기 시작하고, 코어가 GUI의 사정을 알게 되고, 결국 코어만 쓰는 길이 막힌다. 그래서 규칙을 하나 둔다.

> **코어는 앱의 존재를 모른다.** 코어의 어떤 코드도 `#[cfg(feature = "gui")]` 같은 분기를 갖지 않는다. 앱이 필요로 하는 것은 전부 §3 스키마 안에서 표현되어야 하며, 표현할 수 없다면 그것은 앱이 알 필요 없는 것이다.

---

## 2. 비목표 (Non-goals)

명시적으로 하지 않는 것들. 이슈로 올라와도 닫는다.

| 항목 | 이유 |
|---|---|
| 에이전트 실행 / 오케스트레이션 | orca, agent-deck의 영역 |
| git worktree 관리 | 위와 동일 |
| 비용 추적 | 별도 도구가 이미 잘한다 |
| **수집 코어에서 에이전트에 명령 전송** | 코어는 관찰 전용. 향후 waidaway의 사용자 요청 전달은 별도 경로로 구현한다. |
| 히스토리 DB / 시계열 분석 | 지금 상태만 본다 |
| 전체화면 TUI | 아래 참조 |
| 계정, 로그인, 클라우드 저장 | 아래 참조 |
| 에이전트 벤더 API 직접 호출 | 아래 참조 |
| 앱 내 편집 · 터미널 · 파일 뷰어 | 그건 IDE다. 앱이 되었다고 IDE가 되지는 않는다 |

**"원격 머신 / 모바일 앱"을 이 표에서 뺐다.** 대신 그 자리를 세 줄이 채웠다. 비목표를 지울 때는 반드시 그만큼 새로 채워야 한다 — 안 그러면 규율이 증발하고, 규율이 없는 매니페스트는 그냥 위시리스트다.

### 원격을 열되, 무엇을 열지 않는가

waid 수집 코어는 **읽기 전용**을 유지한다. v0.4.0 waidaway는 같은 LAN에서 세션 열람과 지원 대상에 대한 사용자 요청 전달을 목표로 하며, 아직 구현되지 않았다.

- **전달 경로 분리.** 수집 코어는 원본 로그를 수정하거나 명령을 보내지 않는다. waidaway는 사용자가 선택한 지원 세션에만 프롬프트를 전달하며, 대상 식별·인증·암호화·기기 연결 해제·중복 전송 방지를 갖춘 뒤 제공한다. 종료되거나 지원하지 않는 세션은 전달을 거부한다.
- **LAN 우선.** 유선과 Wi-Fi의 같은 네트워크에서 호스트 PC·Mac에 연결한다. 원격 기능은 기본 꺼짐이며, 인터넷 중계 서버는 후속 범위다.
- **벤더 API 없음.** 클라우드 작업을 표에 넣고 싶다면 §4의 피더(feeder) 패턴을 쓴다. 외부 프로그램이 API를 치고 로컬에 파일을 떨구면, waid는 그 파일을 읽는다. 인증 로직이 코어에 들어오지 않는다.

세 번째가 특히 중요하다. `task` 컬럼에는 사용자가 지금 무엇을 만들고 있는지가 문장으로 적혀 있다. 이건 소스 코드보다 유출에 민감할 수 있는 정보다. 그래서 §7이 따로 있다.

### TUI를 넣지 않는 이유

초안에서는 `waid`를 치면 `htop` 같은 전체화면 UI가 뜨는 것을 v0.1 목표로 잡았다. 만들어보고 접었다.

TUI가 표 출력에 더해 주는 것은 **키보드 상호작용**뿐이다. 방향키 스크롤, 정렬 토글, 항목 선택. 그런데 화면에 뜨는 세션은 대개 다섯에서 열 줄이다. 열 줄짜리 목록에 스크롤은 필요 없고, 정렬은 `waiting`이 위로 오는 것 하나면 충분하며(§3.1), 항목을 선택해서 할 일은 애초에 없다 — waid는 개입하지 않으니까(§1.1).

비용은 크다. ratatui와 crossterm, 그리고 그 아래로 딸려오는 의존성이 수십 개다. "외부 크레이트 0개"(§5)와 "바이너리 5 MB 이하"(§8)가 동시에 깨진다. 원칙 두 개를 팔아서 사는 것이 스크롤이라면 남는 장사가 아니다.

상호작용이 정말로 필요한 순간에는 이미 답이 있다:

```sh
waid --json | fzf          # 고르고 싶다면
waid --watch               # 계속 보고 싶다면
waid --html --watch        # 예쁘게 보고 싶다면
```

이것이 §1.3(코어는 렌더링하지 않는다)이 실제로 값을 하는 지점이다. 코어가 JSON만 뱉기 때문에, 코어를 뚱뚱하게 만들지 않고도 원하는 상호작용을 바깥에서 붙일 수 있다.

---

## 3. 출력 스키마

코어가 생산하는 정본(canonical) 형태. 모든 렌더러는 이것만 소비한다.

```json
{
  "schema": 1,
  "captured_at": "2026-09-03T14:22:10+09:00",
  "sessions": [
    {
      "id": "a3f2c891",
      "title": "api-server/auth",
      "agent": { "name": "claude", "display": "Claude Code", "version": "2.1.4" },
      "llm": { "id": "claude-opus-4-6", "display": "opus-4.6" },
      "task": {
        "text": "JWT 리프레시 토큰 만료 처리",
        "source": "transcript_first_prompt",
        "confidence": "inferred"
      },
      "status": { "state": "working", "since": "2026-09-03T14:19:02+09:00" },
      "cwd": "/home/mk/api-server",
      "branch": "feat/auth-refresh",
      "pid": 48213
    }
  ]
}
```

### 3.1 필드별 수집 전략

각 필드가 **어디서 오는가**와 **얼마나 믿을 만한가**를 명시한다. 이게 이 프로젝트의 진짜 난이도다.

#### `agent` — 신뢰도: 확실

실행 파일 basename과 어댑터의 명시적 패키지 표지를 매칭한다 (§4, D2). 어댑터 테이블에서 첫 매치가 이긴다. Windows의 `.cmd`/`.exe` 셔임도 매칭 대상.

#### `llm` — 신뢰도: 높음

우선순위대로 시도:
1. 트랜스크립트 파일 내 `model` 필드 (가장 정확)
2. 커맨드라인 플래그 (`--model`, `-m`)
3. 환경 변수 (`ANTHROPIC_MODEL` 등)
4. 어댑터에 정의된 해당 에이전트의 기본 모델
5. 실패 시 `—` 표시. **추측해서 채우지 않는다.**

#### `status` — 신뢰도: 높음

다섯 가지 상태만 존재한다:

| 상태 | 판정 근거 |
|---|---|
| `working` | 트랜스크립트 마지막 이벤트가 assistant/tool 이고 최근 N초 이내 |
| `waiting` | 마지막 이벤트가 assistant 턴 종료 또는 권한 프롬프트 → **사용자 차례** |
| `idle` | 프로세스는 살아있으나 마지막 이벤트가 임계치보다 오래됨 |
| `done` | 프로세스 종료, 트랜스크립트는 정상 종료 |
| `error` | 비정상 종료 코드 또는 트랜스크립트 내 에러 이벤트 |

`waiting`이 가장 중요한 상태다. 이 도구를 쓰는 실질적 이유가 "어느 놈이 나를 기다리고 있나"이기 때문에, 렌더러는 `waiting`을 항상 최상단에 정렬하고 시각적으로 가장 강조한다.

#### `title` — 신뢰도: 중간

기본값: `<디렉터리 basename>/<브랜치명>`. 브랜치가 기본 브랜치면 디렉터리만.

같은 저장소에서 여러 세션이 동시에 돌면 충돌한다. 해결:
- 리포지토리 루트의 `.waid` 파일이 있으면 그 안의 `title`을 쓴다
- 그래도 겹치면 짧은 세션 id를 접미사로 붙인다 (`api-server/auth·a3f2`)

#### `task` — 신뢰도: 낮음 ⚠️

**이 프로젝트에서 가장 어려운 필드이자, 존재 이유이기도 하다.**

수동 감지 방식에서는 사용자가 무엇을 시켰는지 알 방법이 원칙적으로 없다. 트랜스크립트의 첫 사용자 프롬프트를 긁어오는 게 최선이지만, 실제 프롬프트는 대개 300자짜리 장문이고 그대로 넣으면 표가 무너진다.

폴백 체인:

1. **명시적 라벨** — `WAID_TASK` 환경변수, 또는 `.waid`의 `task` 필드 → `source: "explicit"`
2. **커맨드라인 인자** — `claude -p "..."` 형태의 원샷 실행 → `source: "cmdline"`
3. **트랜스크립트 첫 프롬프트 요약** — 첫 사용자 메시지에서 앞 N자 추출 후 정규화(개행/코드블록/파일 첨부 제거) → `source: "transcript_first_prompt"`
4. **없음** — `—` 표시

`confidence`를 `explicit` / `inferred` / `none` 세 단계로 함께 내보내고, 렌더러는 `inferred`를 흐리게 표시한다. **추론한 값을 확실한 값처럼 보이게 만들지 않는다.**

> 선택적 확장: 로컬 소형 모델로 첫 프롬프트를 한 줄 요약하는 것은 매력적이지만, "네트워크 요청 0회"와 "3초 설치" 원칙을 깬다. v1 범위 밖.

---

## 4. 에이전트 어댑터 규약

새 에이전트 지원은 **코드 수정 없이** 선언적 TOML로 추가된다. 내장 어댑터도 같은 형식이며, `~/.config/waid/adapters/*.toml`이 내장분을 덮어쓴다.

```toml
# ~/.config/waid/adapters/mytool.toml
[adapter]
name    = "mytool"
display = "My Coding Tool"

# 감지 규칙. 정규식이 아니라 실행 파일 이름 목록이다.
exec    = ["mytool", "mytool-cli"]
markers = ["@vendor/mytool"]        # 커맨드라인에 나타나는 패키지 경로 (선택)

[transcript]
dir = "~/.mytool/sessions"          # 선택. 없으면 감지만 하고 llm/task 는 —
```

**정규식을 쓰지 않기로 한 이유.** 초안에서는 커맨드라인 전체를 정규식으로 매칭할 계획이었으나, 구현하면서 그게 틀렸다는 게 드러났다. `vim claude.md`가 Claude Code로 잡히고 `grep -r codex .`도 잡힌다. 우리가 실제로 묻고 싶은 것은 "이 프로세스의 **실행 파일**이 에이전트인가"이므로, argv[0]의 basename을 정규화해 비교한다. 훨씬 좁고 훨씬 정확하다.

부수 효과가 둘 있다. 정규식 크레이트가 의존성 목록에서 빠졌고(§5), 어댑터를 쓰는 사람이 정규식을 배울 필요가 없어졌다. `exec = ["mytool"]`이면 끝이다.

**트랜스크립트 판독은 형식별 코드가 아니라 경로 한 줄로 열린다.** 리더는 JSONL을 훑어 `model`/`model_id`/`modelName`, 첫 사용자 메시지, cwd, 에러 표지를 찾는 일반 로직이다. 어댑터가 `[transcript] dir` 만 주면 `has_reader`가 자동으로 켜진다.

**어댑터 오류는 조용히 무시하되 숨기지 않는다.** 오타 하나로 매 실행마다 stderr가 시끄러워지면 안 되고, 그렇다고 사용자가 "왜 내 어댑터가 안 먹지"에 답을 못 찾아도 안 된다. 파싱 실패는 모아 두었다가 `waid doctor`에서만 보여준다.

**내장 어댑터:** Claude Code, Codex, Gemini CLI, opencode, Aider, Cursor CLI, Copilot CLI, Goose
(트랜스크립트 리더: Claude Code, Codex)

## 5. 아키텍처

```
  ┌──────────────────────────────────────────────────────┐
  │  detectors                                           │
  │  트랜스크립트 스캔 (1차)  +  프로세스 열거 (생존 확인)  │
  └────────────────────────┬─────────────────────────────┘
                           │
  ┌────────────────────────▼─────────────────────────────┐
  │  core  —  waid 바이너리, 외부 크레이트 0개             │
  │  정규화 → Snapshot (§3 스키마)                        │
  │  GUI 코드 없음. 앱의 존재를 모름 (§1.5).               │
  └────────────────────────┬─────────────────────────────┘
                           │  JSON
     ┌─────────────┬───────┴───────┬──────────────┐
     ▼             ▼               ▼              ▼
   table         html          데스크톱 앱      (사용자 정의)
  터미널      브라우저       네이티브 껍데기   stdin으로 JSON
                                   │
                                   │  §7 원격 (선택, 기본 꺼짐)
                                   ▼
                              폰 / 태블릿
```

### 5.1 감지는 트랜스크립트가 주도한다

1판은 프로세스 열거를 1차 신호로 삼았다. 2판에서 뒤집는다. 이유는 하나다 — **프로세스는 실행 환경의 경계를 넘지 못한다.**

WSL 안의 `claude`는 별도 커널 네임스페이스의 리눅스 프로세스다. Windows의 `tasklist`로는 `wsl.exe`밖에 보이지 않는다. 반대도 마찬가지다. PowerShell·Git Bash·WSL·데스크톱 앱에서 각각 돌아가는 세션을 하나의 프로세스 열거로 모으는 방법은 없다.

**파일은 경계를 넘는다.** WSL에서 Windows 파일시스템이 `/mnt/c/...`로 보이고, Windows에서 WSL이 `\\wsl$\<배포판>\home\...`으로 보인다. 에이전트는 어느 쪽에서 돌든 자기 트랜스크립트를 쓴다. 그러니 트랜스크립트 루트를 **여러 개** 훑으면 실행 환경과 무관하게 전부 모인다.

따라서 어댑터(§4)는 트랜스크립트 루트를 목록으로 받는다. 프로세스 열거는 폐기하지 않되 역할이 바뀐다 — 세션 목록을 만드는 주체가 아니라, 이미 찾은 세션이 살아 있는지 확인하는 보조 신호다. 프로세스를 못 보는 환경(원격 볼륨, 샌드박스)에서도 표는 채워진다.

**상태 판정의 1순위는 에이전트가 스스로 남긴 상태 파일이다.** 예컨대 Claude Code는 세션마다 상태 파일을 로컬에 쓴다. 이런 게 있으면 mtime 추론보다 언제나 정확하다. 다만 문서화되지 않은 내부 구조이므로, **있으면 쓰고 없으면 기존 추론으로 떨어지는 폴백**으로만 취급한다. 어느 쪽을 썼는지는 스키마의 `source` 필드에 남긴다.

### 5.2 껍데기는 코어를 링크하고 자체 수집 모드로 실행한다

데스크톱 앱은 플랫폼 기본 컨트롤을 사용하는 작은 네이티브 껍데기다. 하는 일은 셋뿐이다.

1. 같은 EXE를 내부 수집 모드의 자식 프로세스로 띄운다 (`--waid-core --json --watch --history`). 코어는 경로 의존성으로 링크하며 별도 EXE를 동봉하거나 추출하지 않는다. 독립 `waid` CLI도 같은 코어를 사용한다.
2. stdout의 §3 스냅샷을 네이티브 읽기 전용 텍스트 목록으로 표시한다
3. 창의 최소화·닫기·항상 위·불투명도를 담당한다

앱에서는 HTTP 서버·웹뷰·HTML 렌더러를 사용하지 않는다. §6 레이어 3의 클라이언트로서 수집·상태 추론은 코어에 맡기고 표시만 한다. 창 설정은 껍데기 내부 상태이며 에이전트 스키마에 섞지 않는다. 플랫폼별 표시 코드는 허용하되 수집 로직을 복제하지 않는다.

**의존성 예산:** 코어는 표준 라이브러리 외의 의존성을 갖지 않는다. 껍데기에서만 플랫폼 API 바인딩과 JSON 소비용 의존성을 허용한다. 브라우저 엔진이나 범용 UI 프레임워크는 동봉하지 않는다. 코어를 오염시키지 않는다 (§1.5).

---

## 6. 커스터마이징 스펙

세 겹으로 나뉜다. 위로 갈수록 자유도가 높고, 아래로 갈수록 쉽다.

### 레이어 1 — 테마 파일 (제일 흔한 경우)

`~/.config/waid/theme.toml`. 컬럼 선택·순서·폭·정렬과 색상·기호를 바꾼다.

```toml
[columns]
# 순서가 곧 표시 순서. 빼고 싶으면 목록에서 지운다.
order = ["status", "title", "task", "agent", "llm"]

[columns.title]
width = 24
truncate = "middle"      # start | middle | end

[columns.task]
width = 40
dim_when_inferred = true

[sort]
by = ["status", "title"]
status_priority = ["waiting", "working", "error", "idle", "done"]

[symbols]
working = "●"
waiting = "◐"
idle    = "○"
done    = "✓"
error   = "✗"

[colors]
waiting = "#f5a623"      # 가장 눈에 띄어야 한다
working = "#4ade80"
idle    = "#6b7280"
error   = "#ef4444"
# 미지정 시 터미널 기본 팔레트를 따른다
```

### 레이어 2 — HTML 템플릿

`waid --html --template mine.html`. 코어가 스냅샷을 템플릿에 주입한다. CSS는 전적으로 사용자 소유이며, waid는 스타일시트를 강제하지 않는다.

```html
<!-- mine.html -->
<div class="deck">
  {{#each sessions}}
  <article class="card status-{{status.state}}">
    <h3>{{title}}</h3>
    <span class="agent">{{agent.display}}</span>
    <span class="llm">{{llm.display}}</span>
    <p class="task {{#if task.inferred}}dim{{/if}}">{{task.text}}</p>
  </article>
  {{/each}}
</div>
```

`--watch`를 붙이면 SSE로 갱신을 밀어준다. 정적 파일 서빙 외에 서버 기능은 없다.

### 레이어 3 — 임의 렌더러

`waid --json --watch`가 줄 단위 JSON을 stdout으로 흘린다. 여기서부터는 사용자의 세계다. 메뉴바 앱이든, i3blocks 위젯이든, Raspberry Pi에 붙인 e-ink 패널이든.

> **설계 원칙:** 커스터마이징 요청이 들어오면 먼저 "레이어 3으로 충분한가"를 묻는다. 충분하다면 코어에 아무것도 추가하지 않는다.

---

## 7. 원격 접근과 보안

원격은 **기본으로 꺼져 있고, 켜는 데 의식적인 동작이 필요하다.** 켜본 적 없는 사용자에게는 1판과 완전히 동일한 로컬 전용 프로그램이어야 한다.

### 7.1 왜 이 절이 따로 있는가

`task` 컬럼은 사용자가 지금 무엇을 만들고 있는지를 자연어 문장으로 담는다. 미공개 기능 이름, 고객사 이름, 보안 취약점 수정 내용이 여기 그대로 적힌다. **소스 코드보다 유출에 민감할 수 있다.**

1판에서 `--host` 옵션을 만들지 않은 이유가 이것이었다. 2판에서 원격을 여는 이상, 안 만드는 것으로 회피할 수는 없고 제대로 만들어야 한다.

### 7.2 규칙

| 규칙 | 내용 |
|---|---|
| 기본 바인딩 | `127.0.0.1`. 원격은 명시적으로 켜야 열린다 |
| 인증 | 토큰 필수. 토큰 없이 여는 경로를 제공하지 않는다 |
| 페어링 | 앱이 QR을 띄우고 기기가 스캔한다. 토큰을 손으로 옮기게 하지 않는다 |
| 전송 | 평문 HTTP로 LAN에 노출하지 않는다. TLS 또는 터널 위에서만 |
| 만료 | 세션 토큰에 유효기간을 둔다. 영구 토큰은 기본값이 아니다 |
| 표면 | 읽기 엔드포인트만. 쓰기 경로는 존재 자체가 없다(§2) |
| 가시성 | 원격이 켜져 있으면 앱 화면에 항상 표시한다. 조용히 켜져 있는 상태를 만들지 않는다 |

### 7.3 켜지 않고도 되는 길을 먼저 안내한다

SSH를 이미 쓰는 사용자에게는 우리 서버가 필요 없다. 문서는 이쪽을 먼저 보여준다.

```sh
ssh -L 7423:127.0.0.1:7423 devbox     # 포트 포워딩
ssh devbox 'waid --json'              # 터미널이면 이걸로 충분
```

**가장 안전한 기능은 사용자가 켜지 않아도 되는 기능이다.** 원격 모드는 이 길이 막힌 사람을 위한 것이지, 기본 경로가 아니다.

---

## 8. CLI 표면

의도적으로 작다.

```
waid                        표 한 번 출력 (기본, TUI 아님)
waid -1, --once             한 번 출력하고 종료
waid --json                 JSON 스냅샷 (--once 함의)
waid --watch                지속 스트리밍 (--json/--html과 조합)
waid --html [--template F]  HTML 출력
waid -i, --interval SEC     폴링 주기 (기본 2)
waid --agent NAME           특정 에이전트만 필터
waid --waiting              waiting 상태만 (스크립트용)
waid doctor                 감지된 어댑터/경로 진단
```

서브커맨드는 `doctor` 하나로 끝. `waid start`, `waid kill` 같은 건 없다 (§1.1).

---

## 9. 성능 예산

"가볍다"를 주장이 아니라 **측정 가능한 숫자**로 못 박는다. CI에서 회귀를 검사한다.

예산을 **코어와 앱으로 분리한다.** 앱이 무거워지는 것을 코어가 무거워질 핑계로 쓰지 못하게 하기 위해서다.

**코어 (`waid` 바이너리)** — 1판 예산 그대로. 완화하지 않는다.

| 항목 | 예산 | 현재 |
|---|---|---|
| 바이너리 크기 | < 5 MB (정적 링크, 단일 파일) | Windows x64 407,552 bytes |
| 상주 메모리 (RSS) | < 20 MB, 세션 20개 기준 | Windows 실앱 코어 working set 약 7.2 MB; 고정 20세션 시험 별도 |
| 콜드 스타트 → 첫 출력 | < 120 ms | Windows 반복 5회 82.5~140.1ms; 초과 사례 있음, 콜드 보증 아님 |
| 유휴 CPU | < 0.5%, 2초 폴링 기준 | Windows 측정·검증 중 |
| 외부 크레이트 | 0개 | 0개 |
| 자발적 네트워크 요청 | 0회. 영구히 | 0회 |

마지막 줄이 **자발적**으로 바뀐 것에 주의. 코어는 여전히 스스로 바깥에 연결하지 않는다. §7의 원격 서버는 들어오는 연결을 받을 뿐이고, 그마저도 기본으로 꺼져 있다.

**앱 (네이티브 껍데기)**

| 항목 | 예산 |
|---|---|
| 설치 파일 크기 | < 15 MB (브라우저·웹뷰 의존성 없음) |
| 유휴 메모리 | < 150 MB |
| 유휴 창 CPU | < 1% |
| 앱 전용 렌더링 코드 | 플랫폼 기본 컨트롤과 §3 JSON 표시만. 수집·추론 중복 없음 |

트랜스크립트는 전체를 읽지 않는다. 파일 끝에서 역방향으로 필요한 만큼만 읽고, 첫 프롬프트는 최초 1회만 읽어 캐시한다.

2026-09-05 네이티브 앱 v0.5.1은 240,128 bytes, NSIS는 297,625 bytes다. 설치본 관측 3회에서 앱·코어·콘솔 호스트 working set 합은 약 34.7 MB였다. 순간적인 tasklist 실행을 모두 포착한 최대치나 고정 20세션 시험은 아니다. 이전 WebView2 앱의 약 157 MB private working set 기록은 D19의 교체 근거로 남긴다. CPU·콜드 스타트 예산 충족은 여전히 완료하지 않았다. 세부 조건은 DECISIONS.md 마지막 절에 기록하며 예산을 완화하지 않는다.

---

## 10. 로드맵

**실환경 검증 순서: Windows → macOS → Android → WSL.** Windows에서 당장 사용할 MVP를 먼저 검증한다. Linux 구현은 유지하며, WSL 재검증은 마지막으로 미룬다. WSL에서 Windows Codex 트랜스크립트를 읽은 기존 실환경 검증은 사용자 보고로 기록한다.

**v0.2.0 — Windows 사용 가능 단계**
독립 실행 앱, 세션 수집·검색·정리·복귀. 안정화 검증은 계속한다. 'waid에서 보지 않기'는 원본 세션을 삭제하지 않고 기본 목록에서만 제외하며, 전체 보기에서 확인하고 새 사용자 요청이 감지되면 복귀한다.

**v0.3.0 — macOS 지원**
Windows의 핵심 세션 관리 경험을 Mac 네이티브 앱으로 제공한다. 수집·정리·재시작 후 설정 유지·지원 대상 세션 복귀를 깨끗한 Mac에서 검증한다. Apple Silicon·Intel 코어 및 공통 로직의 자동 검사는 중간 단계이며, 최소 macOS 버전·설치·서명·공증·실제 Claude/Codex 로그 검증을 거쳐 출시 범위를 확정한다.

**v0.4.0 — Android·waidaway LAN**
기존 세션 하나에 정확히 입력을 전달하는 실험부터 시작한다. 그다음 모바일 브라우저 기반 LAN 열람·페어링·인증·암호화·연결 해제를 구현하고, 지원 세션에 대한 프롬프트 전달·결과 표시·중복 전송 방지를 검증한다. 조회 가능 여부와 입력 가능 여부는 별도로 표시한다.

**v0.5.0 — iOS·iPadOS 지원**
iPhone·iPad에서 모바일 세션 관리 경험을 제공한다. macOS 지원에 포함되지 않는 별도 단계다.

**그 다음 — WSL 회귀 검증**
기존 Linux 경로를 유지하고 WSL → Windows 및 Windows → WSL 트랜스크립트 수집을 실환경에서 확인한다. 앞선 Windows·macOS·Android 검증을 이 작업 때문에 미루지 않는다.

**v1.0.0 — 안정화 완료**
지원 플랫폼과 핵심 사용 흐름의 호환성·신뢰성·실사용 검증을 완료한 단계다. 기능 추가만으로 달성하지 않으며, 이후 기능 확장을 금지하는 의미도 아니다.

> 1판에는 TUI가 있었다. 만들어보고 §2로 옮겼다. 로드맵에서 무언가를
> 지우는 것도 진척이다.

---

## 11. 성공의 정의

이 프로젝트가 성공했다는 것은:

- 설치하고 30초 안에 유용한 화면이 나온다
- 어느 셸에서 에이전트를 띄웠든 **한 화면에 다 뜬다** (2판의 핵심 약속)
- 사람들이 자기 테마를 공유하기 시작한다
- 기능 요청의 절반쯤을 "레이어 3으로 하시면 됩니다"로 답할 수 있다
- 앱을 지우고 `waid` 바이너리만 남겨도 그대로 쓸 만하다
- 원격을 한 번도 켜지 않은 사용자가 불편을 느끼지 않는다

실패했다는 것은:

- 세션을 실행하는 기능이 들어간다
- 컬럼이 여덟 개가 된다
- 설정 파일 없이는 쓸 수 없어진다
- **코어가 앱 없이는 못 돌게 된다**
- **원격이 기본값이 되거나, 켜져 있는 줄 모르는 사용자가 생긴다**
- 로그인 화면이 생긴다
